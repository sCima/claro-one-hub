'use client';

import { AppChannel } from '@/src/components/channels/AppChannel';

/* Canal App (simulação) — Documento §4.1, canal_origem 'app'.
 * Mesma sessão do Web Chat / WhatsApp. */
export default function AppChannelPage() {
  return <AppChannel />;
}
